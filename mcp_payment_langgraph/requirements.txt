# MCP Payment LangGraph - Updated Dependencies
# Core MCP and LangGraph dependencies
mcp>=1.12.4
langgraph>=0.6.4
langchain-core>=0.3.74

# HTTP API dependencies (for optional FastAPI server)
fastapi>=0.116.1
uvicorn>=0.35.0

# Data handling and validation
pydantic>=2.11.0
faker>=22.0.0
python-dateutil>=2.9.0.post0

# Network and utilities
httpx>=0.28.1
graphviz>=0.21

# Installation:
# uv sync (for UV users - recommended)
# pip install -r requirements.txt (for pip users)

# Usage:
# uv run main.py (primary MCP server for Claude Desktop)
# python mcp_server.py (alternative MCP server)
